name: Doxygen deployment

on:
  push:
    branches: [ "master" ]

jobs:
  doxygen:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Installing Doxygen
      run: apt-get update
